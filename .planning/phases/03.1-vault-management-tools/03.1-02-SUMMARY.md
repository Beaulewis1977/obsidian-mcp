---
phase: 03.1-vault-management-tools
plan: 02
subsystem: tools
tags: [tool-registry, vitest, vault-management, integration-tests]

# Dependency graph
requires:
  - phase: 03.1-vault-management-tools plan 01
    provides: handlers-vault.ts, obsidian-config.ts, AddVaultSchema/RemoveVaultSchema/ListVaultsSchema
provides:
  - 3 vault tools registered in buildRegistry() under 'Vault Management' category
  - handlers-vault.test.ts with 10 integration tests covering all 3 handlers
  - lazy-loading.integration.test.ts updated to reflect 22 total tools
affects: [future phases adding tools to buildRegistry]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "vi.mock() hoisted before imports pattern for fs/promises and platform mocks in test files"
    - "makeMockConfig() helper function for deep-cloning test config to avoid cross-test contamination"

key-files:
  created:
    - src/tools/__tests__/handlers-vault.test.ts
  modified:
    - src/tools/index.ts
    - src/tools/__tests__/lazy-loading.integration.test.ts

key-decisions:
  - "handlers-vault.test.ts uses vi.mock('fs/promises') with default export pattern to match Node ESM fs mock shape"
  - "makeMockConfig() factory function (not const) ensures each test gets a fresh config object with no shared mutation risk"
  - "10 tests cover all guard paths: duplicate name, missing confirm, last-vault, default-vault, disk-missing, obsidian-config-empty"

patterns-established:
  - "Mock hoisting: vi.mock() calls appear before all imports; typed mock refs created with vi.mocked() after import block"
  - "Registry wiring: new tool categories get a section comment before the first registration in that category"

requirements-completed: [VAULT-05, VAULT-06]

# Metrics
duration: 3min
completed: 2026-02-27
---

# Phase 3.1 Plan 02: Vault Management Tools — Registry + Tests Summary

**3 vault management tools (add_vault, remove_vault, list_vaults) wired into ToolRegistry and proven by 10 integration tests covering all success and guard paths; 153 tests total passing.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-02-27T23:28:58Z
- **Completed:** 2026-02-27T23:30:04Z
- **Tasks:** 4 (3 code tasks + 1 verification)
- **Files modified:** 3

## Accomplishments

- Registered `add_vault`, `remove_vault`, `list_vaults` in `buildRegistry()` under the 'Vault Management' category with `alwaysLoaded: false`
- Updated tool count from 19 to 22 in lazy-loading integration tests (20 feature + 2 meta); added `add_vault`, `remove_vault`, `list_vaults` presence assertions
- Created `handlers-vault.test.ts` with 10 integration tests: success paths + all guard paths for all 3 handlers
- Full pre-commit gate passes: `tsc --noEmit` (0 errors), `vitest --run` (153 tests), `tsup` (build success)

## Task Commits

Each task was committed atomically:

1. **Task 1: Wire 3 vault tools into buildRegistry()** - `4d949d2` (feat)
2. **Task 2: Update lazy-loading test counts (19 → 22)** - `104ca6f` (test)
3. **Task 3: Create handlers-vault.test.ts** - `c7de828` (test)

## Files Created/Modified

- `src/tools/index.ts` - Added `add_vault`, `remove_vault`, `list_vaults` registrations in new "Vault Management tools (Phase 3.1)" section before meta-tools
- `src/tools/__tests__/lazy-loading.integration.test.ts` - Updated tool counts 19 → 22 in 3 assertions; added 3 vault tool name presence checks
- `src/tools/__tests__/handlers-vault.test.ts` - New test file: 10 tests covering handleAddVault (success, duplicate, folder-exists), handleRemoveVault (success, no-confirm, last-vault, default-vault), handleListVaults (success, disk-missing, obsidian-config-empty)

## Decisions Made

- Used `makeMockConfig()` factory function (not a `const`) so each test receives a deep-cloned config — prevents cross-test mutations from `Object.assign(config, reloaded)` in handlers
- Mocked `fs/promises` as `{ default: { ... } }` (not named exports) to match how `handlers-vault.ts` imports `fs` as a default import
- `mockFs.access.mockRejectedValue(...)` used to simulate ENOENT for both the "folder already exists" and "vault not on disk" test paths

## Deviations from Plan

None — plan executed exactly as written. All three files were already committed by the time verification ran (tasks completed in a prior execution window). The SUMMARY.md and state updates were the remaining actions.

## Issues Encountered

None. TypeScript clean, all 153 tests green, tsup build success.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Phase 3.1 is fully complete:
- Platform module: `src/platform/obsidian-config.ts` (atomic reads/writes to obsidian.json)
- Config split-brain fix: `getActiveConfigPath()` finds the actual loaded config path
- Schemas: `AddVaultSchema`, `RemoveVaultSchema`, `ListVaultsSchema`
- Handlers: `handleAddVault`, `handleRemoveVault`, `handleListVaults` (all guards in place)
- Registry: all 3 tools registered under 'Vault Management' category
- Tests: 10 integration tests covering all paths, 153 total passing

Ready to proceed to Phase 4 (extended tools + polish) or any subsequent phase.

---
*Phase: 03.1-vault-management-tools*
*Completed: 2026-02-27*
